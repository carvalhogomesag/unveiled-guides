---
title: "Your Expert Guides to the World's Greatest Landmarks"
layout: "base.njk"
---

{# Título da secção principal de guias #}
<h2 class="section-title">All Our Guides</h2>

<div class="guides-grid">
    {% for post in collections.post %}
        {# Cada card é um link para o artigo completo #}
        <a href="{{ post.url }}" class="guide-card-link">
            {# A imagem de fundo é definida dinamicamente a partir do front matter #}
            <article class="guide-card" style="background-image: url('{{ post.data.featuredImage }}');">
                <div class="guide-card-content">
                    
                    {# --- ESTA É A ALTERAÇÃO CRÍTICA --- #}
                    {# Novo container para as categorias #}
                    <div class="guide-card-categories">
                        {# Verifica se a lista 'categories' existe e não está vazia #}
                        {% if post.data.categories and post.data.categories.length %}
                            {# Percorre a lista de categorias, mas limita a no máximo 2 #}
                            {% for category in post.data.categories | limit(2) %}
                                <span class="guide-card-category">{{ category }}</span>
                            {% endfor %}
                        {% endif %}
                    </div>

                    <h3 class="guide-card-title">{{ post.data.title }}</h3>

                </div>
            </article>
        </a>
    {% endfor %}
</div>